let products = ["Chocolate", "Candy", "Ice Cream", "Brownie"];
let counter = 0;
while (counter < products.length) {
  //console.log(products[counter]);
  //counter=0 , products.length=4, products[0];
  //counter=1 , products.length=4, products[1];
  //counter=2 , products.length=4, products[2];
  //counter=3 , products.length=4, products[3];
  counter++;
}

for (counter = 0; counter < products.length; counter++) {
  console.log(products[counter]);
}
